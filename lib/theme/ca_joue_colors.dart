import 'package:flutter/painting.dart';

/// A class containing the color palette for the Ça Joue application.
class CaJoueColors {
  const CaJoueColors._();

  /// Off-white background (#F5F3EE).
  static const Color snow = Color(0xFFF5F3EE);

  /// Warm light background (#EBE8E1).
  static const Color cream = Color(0xFFEBE8E1);

  /// Primary dark text (#1A1A1C).
  static const Color slate = Color(0xFF1A1A1C);

  /// Secondary muted text (#7A7A7E).
  static const Color stone = Color(0xFF7A7A7E);

  /// Error / incorrect answer (#D42D2D).
  static const Color red = Color(0xFFD42D2D);

  /// Accent gold (#C4A265).
  static const Color gold = Color(0xFFC4A265);

  /// Gold at 12% opacity for soft backgrounds.
  static const Color goldSoft = Color(0x1FC4A265);

  /// Gold at 25% opacity for borders.
  static const Color goldBorder = Color(0x40C4A265);

  /// Warm brown accent (#B8937A).
  static const Color dusk = Color(0xFFB8937A);

  /// Neutral warm grey (#C8C3B8).
  static const Color warmGrey = Color(0xFFC8C3B8);

  /// Warm light accent (#D4C5A9).
  static const Color morning = Color(0xFFD4C5A9);
}
